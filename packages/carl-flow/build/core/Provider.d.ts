import React from 'react';
import { Store } from './store';
declare type CarlFlowStore = {
    store: Store;
};
declare const _default: (props: React.PropsWithChildren<CarlFlowStore>) => JSX.Element;
export default _default;
